function mostrar()
{
var clave = prompt("Ingrese el numero clave ");


//validamos datos

while ( clave !="utn750") // while( !(clave = "utn750")) ! = not
{
    clave = prompt("Clave incorrecta, ingrese de vuelta la clase");

}//FIN DE LA FUNCIÓN


}
